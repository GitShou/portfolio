# scripts

リポジトリ直下の `scripts/` は「開発・運用の補助スクリプト置き場」です。

## 内容
- `collect-lambda-logs.ps1`: CloudWatch Logs から Lambda ログを収集して `scripts/log/` に保存
- `move-aws-icons.ps1`: AWS 公式アイコンセットから必要な SVG を抽出して配置
- `log/`: 生成物 (ログ出力先)
